# Migrations — app schema (later)

App-owned tables (e.g. **inventory**) will be added later, likely via an ORM in the API rather than raw SQL migrations here. The MTGJSON card catalog is loaded separately via `scripts/load-mtgjson.ps1`.
