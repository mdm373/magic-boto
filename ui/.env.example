# Chat UI (ChatRaw) — API URL is set in the app (Settings → Model Settings), not via env.
# This file is for reference only. ChatRaw does not read OPENAI_API_BASE_URL from env;
# configure API Base URL and Model ID once in the UI after first load.
